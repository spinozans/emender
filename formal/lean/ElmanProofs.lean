-- Emender paper-facing trusted proof root.

import ElmanProofs.PaperCore

/-!
The `ElmanProofs` namespace name is historical and is preserved here to keep
the proof artifact reproducible against the original `elman-proofs` lineage.

The public Emender repository imports only `ElmanProofs.PaperCore`. Historical
proof sketches and retired overclaims live in the provenance repository rather
than in this paper-facing artifact.
-/
