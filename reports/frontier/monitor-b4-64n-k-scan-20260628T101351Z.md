# Monitor B4 64-node K Scan Jobs

Task: `monitor-b4-64n`  
Report time: `2026-06-28T10:13:51Z` (`2026-06-28 06:13:51 EDT`)

## Decision

Do not launch a replacement 24h production run yet.

Both corrected `BATCH_SIZE=4` 64-node scan jobs are still scheduler-blocked in
`PENDING` state with reason `Priority`, zero allocated nodes, no stdout/stderr
files, and no run roots under the shared scaleout run directory. There is no
runtime loss, throughput, merge, memory, or failure evidence on which to select
`B4 K10` or `B4 K20`.

Replacement recommendation: **no replacement yet**. Keep the provisional
preference from `cancel-b1-24h`: if both arms later run cleanly, prefer `B4 K20`
as the less aggressive lower-merge-frequency candidate; choose `B4 K10` only if
it is materially cleaner or K20 fails for a reason unrelated to B4 safety.

## Scheduler State

Commands checked:

```bash
sacct -j 4910703,4910704 --format=JobIDRaw,JobID,JobName%40,State,Reason%30,ExitCode,Submit,Eligible,Start,End,Elapsed,NNodes,NodeList%64,ReqTRES%80,AllocTRES%80 -P
squeue -j 4910703,4910704 -o '%i|%j|%T|%r|%S|%M|%D|%R|%V'
scontrol show job 4910703
scontrol show job 4910704
```

Latest observed scheduler state:

| Arm | Job ID | State | Blocking reason | Submit / eligible | Estimated start | Estimated end | Nodes requested | Nodes allocated | Exit code | Elapsed |
| --- | --- | --- | --- | --- | --- | --- | ---: | ---: | --- | --- |
| B4 K10 | `4910703` | `PENDING` | `Priority` | `2026-06-28T06:00:22` | `2026-06-28T15:42:00` | `2026-06-28T16:27:00` | 64 | 0 | `0:0` | `00:00:00` |
| B4 K20 | `4910704` | `PENDING` | `Priority` | `2026-06-28T06:00:22` | `2026-06-28T16:12:00` | `2026-06-28T16:57:00` | 64 | 0 | `0:0` | `00:00:00` |

`sacct` still shows `Start=Unknown`, `End=Unknown`, `NodeList=None assigned`,
and empty `AllocTRES` for both jobs. `squeue` and `scontrol` expose backfill
start estimates, but neither job has started.

Requested resources for each arm:

| Field | Value |
| --- | --- |
| Partition / account | `batch` / `bif148` |
| Nodes / tasks | 64 nodes, 512 tasks, 8 tasks per node |
| CPUs | 3,584 total, 7 per task |
| GPUs | 1 GPU per task |
| Requested TRES | `billing=3584,cpu=3584,mem=32000G,node=64` |
| Network | `disable_rdzv_get` |
| Time limit | `00:45:00` |

## Paths

The jobs were submitted from the `cancel-b1-24h` worktree, with a stable runtime
repo exported:

```text
SubmitLine=sbatch --network=disable_rdzv_get --export=ALL,REPO=/lustre/orion/bif148/scratch/erikgarrison/emender ...
WorkDir=/lustre/orion/bif148/scratch/erikgarrison/emender/.wg-worktrees/agent-411
```

Slurm script paths:

```text
/lustre/orion/bif148/scratch/erikgarrison/emender/.wg-worktrees/agent-411/scripts/frontier/e97_1p3b_step483000_b4_k10_64n_scan.sbatch
/lustre/orion/bif148/scratch/erikgarrison/emender/.wg-worktrees/agent-411/scripts/frontier/e97_1p3b_step483000_b4_k20_64n_scan.sbatch
```

Repository versions of the wrappers:

```text
scripts/frontier/e97_1p3b_step483000_b4_k10_64n_scan.sbatch
scripts/frontier/e97_1p3b_step483000_b4_k20_64n_scan.sbatch
```

Reserved stdout/stderr paths from `scontrol`:

| Arm | stdout | stderr | Present at report time |
| --- | --- | --- | --- |
| B4 K10 | `/lustre/orion/bif148/scratch/erikgarrison/emender/.wg-worktrees/agent-411/logs/frontier/scaleout/e97-s483-b4-k10-64n-4910703.out` | `/lustre/orion/bif148/scratch/erikgarrison/emender/.wg-worktrees/agent-411/logs/frontier/scaleout/e97-s483-b4-k10-64n-4910703.err` | no |
| B4 K20 | `/lustre/orion/bif148/scratch/erikgarrison/emender/.wg-worktrees/agent-411/logs/frontier/scaleout/e97-s483-b4-k20-64n-4910704.out` | `/lustre/orion/bif148/scratch/erikgarrison/emender/.wg-worktrees/agent-411/logs/frontier/scaleout/e97-s483-b4-k20-64n-4910704.err` | no |

Expected run-root pattern after allocation:

```text
/lustre/orion/bif148/proj-shared/emender/frontier_runs/scaleout/<UTC_DATE>/E97_1.3B_step483000_b4_k10_64n_hier_g4_bucket64m_avg_scan/<JOB>-<STAMP>
/lustre/orion/bif148/proj-shared/emender/frontier_runs/scaleout/<UTC_DATE>/E97_1.3B_step483000_b4_k20_64n_hier_g4_bucket64m_avg_scan/<JOB>-<STAMP>
```

No matching `b4_k10_64n` or `b4_k20_64n` run directories or files were found
under `/lustre/orion/bif148/proj-shared/emender/frontier_runs/scaleout` at
report time.

## Recipe Manifest

Both arms resume from:

```text
/lustre/orion/bif148/proj-shared/emender/checkpoints/E97_1.3B_20260623_103742_step_483000/checkpoint_step_483000_loss_2.5431.pt
```

Common recipe:

| Field | Value |
| --- | --- |
| Per-GPU batch | `BATCH_SIZE=4` |
| Chunk size | `2048` |
| Train minutes | `28` |
| DiLoCo island size | `1` |
| Outer optimizer | `avg`, `outer_lr=1.0`, `outer_beta=0.0` |
| Export basis | `x` |
| Merge topology | `hierarchical` |
| Merge group size | `4` |
| Merge bucket | `67108864` elements |
| RCCL env | `FRONTIER_RCCL_ENV=recommended`, `FRONTIER_RCCL_ALT_RDZV=1` |
| Compile warmup | `1` step |
| Keep checkpoints | `4` |

Arm-specific settings:

| Arm | `SCALEOUT_VARIANT` | `DILOCO_K` | Save/check interval |
| --- | --- | ---: | --- |
| B4 K10 | `E97_1.3B_step483000_b4_k10_64n_hier_g4_bucket64m_avg_scan` | 10 | `SAVE_EVERY=10`, walltime/health check every 10 |
| B4 K20 | `E97_1.3B_step483000_b4_k20_64n_hier_g4_bucket64m_avg_scan` | 20 | `SAVE_EVERY=20`, walltime/health check every 20 |

Runtime `artifacts/env.txt`, `artifacts/manifest.json`, `logs/train.log`, and
`summaries/summary.md` are unavailable because the jobs have not allocated and
no run roots exist.

## Metrics Summary

| Metric | B4 K10 `4910703` | B4 K20 `4910704` |
| --- | --- | --- |
| Final Slurm state | not final: `PENDING` / `Priority` | not final: `PENDING` / `Priority` |
| Allocation | none, `AllocNodes=0` | none, `AllocNodes=0` |
| stdout/stderr | reserved paths only; files absent | reserved paths only; files absent |
| Run root | absent | absent |
| Env / manifest | unavailable | unavailable |
| Train log | unavailable | unavailable |
| Summary | unavailable | unavailable |
| First loss window | unavailable | unavailable |
| Last loss window | unavailable | unavailable |
| Global tok/s | unavailable | unavailable |
| Per-GPU tok/s estimate | unavailable | unavailable |
| Merge count | unavailable | unavailable |
| Merge duration | unavailable | unavailable |
| Memory summary | unavailable | unavailable |
| Severe failure scan | no runtime logs to scan; scheduler metadata only | no runtime logs to scan; scheduler metadata only |

Throughput cannot be estimated from this monitor pass because there are no rank-0
metric rows. Per-GPU tok/s also cannot be derived because global tok/s is absent;
when available, use `global_tok/s / 512` for the 64-node, 512-rank estimate.

## Severe Failure Scan

Runtime severe-failure scan terms requested:

```text
memory OOM Traceback Exception NCCL RCCL timeout nan non-finite
```

No stdout, stderr, `train.log`, or summary files exist for either corrected job
yet. Therefore the scan found no runtime evidence, but this is not a clean-run
signal; it only confirms scheduler blocking before allocation.

## Next Monitor Trigger

Re-run the monitor after either job leaves `PENDING`, or after the current
backfill estimates:

| Arm | Recheck after |
| --- | --- |
| B4 K10 | `2026-06-28T16:27:00Z` |
| B4 K20 | `2026-06-28T16:57:00Z` |

At that point harvest the exact stdout/stderr files, run roots, env/manifest,
train logs, summaries, loss windows, throughput, per-GPU tok/s, merge
count/durations, memory summaries, final checkpoint status, and severe failure
scan before selecting a replacement.
